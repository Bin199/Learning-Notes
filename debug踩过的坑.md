debug踩过的坑

***

1.代码当中有时会设置一个cache文件用于存储已处理好的输入数据，主要是feature和label（防止重复处理太麻烦），第一次跑代码时从原数据文件得到输入数据，然后存在cache文件中，从第二次开始就直接从cache文件加载输入数据了，但如果这时你对训练需要的数据做了改动，理论上需要重新从原始数据中处理得到输入数据，但模型并不知道训练数据发生改变，依旧从cache文件加载，便会出现错误，比如

RuntimeError: CUDA error: device-side assert triggered

这种错误不易发现，非常坑！！！！